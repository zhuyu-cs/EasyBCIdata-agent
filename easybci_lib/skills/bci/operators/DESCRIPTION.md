Atomic preprocessing operators for BCI neural data.
Each operator is a single processing step (filter, reference, scale, etc.)
registered by applicable modality. Load an operator skill when you need
detailed parameter guidance for a specific preprocessing step.
